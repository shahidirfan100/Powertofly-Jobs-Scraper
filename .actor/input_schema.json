{
  "title": "PowerToFly Jobs Scraper",
  "description": "Extract job listings from PowerToFly with complete details including salary, job type, location, and full descriptions. Search by keywords, location, or provide a direct PowerToFly URL.",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "startUrl": {
      "title": "Start URL",
      "type": "string",
      "description": "Start scraping from a specific PowerToFly jobs URL. If provided, this will override keyword and location parameters.",
      "editor": "textfield"
    },
    "keyword": {
      "title": "Search Keywords",
      "type": "string",
      "description": "Job search keywords such as job titles, skills, or technologies (e.g., 'Software Engineer', 'Data Scientist', 'Product Manager').",
      "editor": "textfield",
      "prefill": "software engineer"
    },
    "location": {
      "title": "Location",
      "type": "string",
      "description": "Filter jobs by location (e.g., 'New York', 'Remote', 'San Francisco'). Leave empty to search all locations.",
      "editor": "textfield"
    },
    "category": {
      "title": "Category",
      "type": "string",
      "description": "Filter jobs by category (e.g., 'Engineering', 'Data', 'Marketing'). Leave empty to search all categories.",
      "editor": "textfield"
    },
    "sortByPublished": {
      "title": "Sort By Published Date",
      "type": "boolean",
      "description": "When true, the newest published jobs are returned first.",
      "editor": "checkbox",
      "default": true
    },
    "collectDetails": {
      "title": "Collect Full Job Details",
      "type": "boolean",
      "description": "When enabled, the scraper visits each job detail page to extract complete information including full descriptions, salary, and job type.",
      "editor": "checkbox",
      "default": true
    },
    "dedupe": {
      "title": "Dedupe By Job ID",
      "type": "boolean",
      "description": "Remove duplicate job IDs across pages before saving.",
      "editor": "checkbox",
      "default": true
    },
    "results_wanted": {
      "title": "Maximum Results",
      "type": "integer",
      "description": "Maximum number of job listings to extract. Set a reasonable limit to control run time and costs.",
      "minimum": 1,
      "default": 100,
      "editor": "number"
    },
    "max_pages": {
      "title": "Maximum Pages",
      "type": "integer",
      "description": "Safety limit on the number of API pages (size/from windows) to crawl. Prevents excessive resource usage.",
      "minimum": 1,
      "editor": "number",
      "default": 20
    },
    "cookies": {
      "title": "Custom Cookies (Header String)",
      "type": "string",
      "description": "Cookies to send with every request, as a single header string (e.g., \"a=1; b=2\").",
      "editor": "textfield"
    },
    "cookiesJson": {
      "title": "Custom Cookies (JSON)",
      "type": "string",
      "description": "Cookies as JSON object or array of {\"name\":\"\",\"value\":\"\"}. Useful when pasting exports from browser devtools.",
      "editor": "textarea"
    },
    "proxyConfiguration": {
      "title": "Proxy Configuration",
      "type": "object",
      "description": "Configure proxy settings. Residential proxies are recommended for reliable access to PowerToFly.",
      "editor": "proxy",
      "default": {
        "useApifyProxy": true,
        "apifyProxyGroups": ["RESIDENTIAL"]
      }
    }
  },
  "additionalProperties": false
}
